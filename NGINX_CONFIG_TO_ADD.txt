# ==============================================================
# NGINX CONFIGURATION FOR LEMURDX-DASHBOARD-DATA
# ==============================================================
#
# File to edit: /etc/nginx/sites-available/default
# Command: sudo nano /etc/nginx/sites-available/default
#
# Find the server block around line 75 that looks like this:
#
#   server {
#       listen 443 ssl;
#       server_name big1.lan.cmu.edu;
#
#       # ... SSL config ...
#
#       location /lemurdx { ... }
#       location /lemurdx/watch_upload { ... }
#       location /prism { ... }
#       location /cleveland { ... }
#       location /cleveland_dashboard { ... }
#       location /prism_chat {
#           proxy_pass http://localhost:5007;
#           proxy_set_header Host $host;
#           proxy_set_header X-Real-IP $remote_addr;
#           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       }
#
#       <<< ADD THE BLOCK BELOW HERE >>>
#
#   }  # <-- Don't forget this closing brace exists!
#
# ==============================================================
# COPY AND PASTE THE FOLLOWING BLOCK:
# ==============================================================

    location /lemurdx-dashboard-data {
        proxy_pass http://127.0.0.1:5008;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Prefix /lemurdx-dashboard-data;

        # WebSocket support (if needed)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

# ==============================================================
# AFTER ADDING THE BLOCK ABOVE:
# ==============================================================
#
# 1. Save and exit (Ctrl+O, Enter, Ctrl+X in nano)
#
# 2. Remove the conflicting config:
#    sudo rm /etc/nginx/sites-enabled/lemurdx-dashboard-data_nginx.conf
#    sudo rm /etc/nginx/sites-available/lemurdx-dashboard-data_nginx.conf
#
# 3. Test nginx config:
#    sudo nginx -t
#
# 4. If test passes, reload nginx:
#    sudo systemctl reload nginx
#
# ==============================================================
